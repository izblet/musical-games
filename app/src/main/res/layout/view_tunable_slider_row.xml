<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rowRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/editable_row_background"
    android:padding="@dimen/editable_row_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/editButton"
            android:layout_width="@dimen/icon_button_size_small"
            android:layout_height="@dimen/icon_button_size_small"
            android:layout_marginEnd="@dimen/editable_row_icon_gap"
            android:padding="@dimen/icon_button_padding_small"
            android:background="@drawable/edit_toggle_background"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Edit"
            android:src="@drawable/ic_pencil" />

        <TextView
            android:id="@+id/label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <com.example.musicalgames.utils.components.ui_components.InfoIconButton
            android:id="@+id/infoButton"
            android:layout_width="@dimen/icon_button_size_small"
            android:layout_height="@dimen/icon_button_size_small"
            android:layout_marginStart="4dp"
            android:padding="@dimen/icon_button_padding_small"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <com.google.android.material.slider.Slider
        android:id="@+id/slider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageButton
            android:id="@+id/decreaseButton"
            android:layout_width="@dimen/icon_button_size_small"
            android:layout_height="@dimen/icon_button_size_small"
            android:padding="@dimen/icon_button_padding_small"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Decrease"
            android:enabled="false"
            android:tint="@color/disableable_icon_tint"
            android:src="@drawable/ic_arrow_left" />

        <TextView
            android:id="@+id/valueText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="32dp"
            android:minWidth="64dp"
            android:textAlignment="center" />

        <ImageButton
            android:id="@+id/increaseButton"
            android:layout_width="@dimen/icon_button_size_small"
            android:layout_height="@dimen/icon_button_size_small"
            android:padding="@dimen/icon_button_padding_small"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Increase"
            android:enabled="false"
            android:tint="@color/disableable_icon_tint"
            android:src="@drawable/ic_arrow_right" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/resetButton"
        android:layout_width="@dimen/icon_button_size_small"
        android:layout_height="@dimen/icon_button_size_small"
        android:layout_gravity="center_horizontal"
        android:padding="@dimen/icon_button_padding_small"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Reset to default"
        android:scaleX="-1"
        android:enabled="false"
        android:tint="@color/disableable_icon_tint"
        android:src="@drawable/ic_reset" />

</LinearLayout>
